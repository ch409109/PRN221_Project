@page
@model BookingTicketOnline.Pages.Category.CreateCategoryModel
@{
    Layout = "_AdminLayout";
}
<div class="container my-5" style="max-width: 600px">
    <h2 class="text-center mb-4">Create Category</h2>
    <form>
        <div class="mb-3">
            <label for="username" class="form-label">Name</label>
            <input type="text" class="form-control" id="username" placeholder="Enter category name" required>
        </div>
        <div class="d-flex justify-content-between">
            <button type="submit" class="btn btn-primary" asp-page="/Category/ManageCategories">Save</button>
            <a type="button" class="btn btn-outline-secondary" asp-page="/Category/ManageCategories">Cancel</a>
        </div>
    </form>
</div>
