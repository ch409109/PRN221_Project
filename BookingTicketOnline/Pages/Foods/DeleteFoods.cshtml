@page
@model BookingTicketOnline.Pages.Foods.DeleteFoodsModel
@{
    Layout = "_AdminLayout";
}

<div class="container my-5" style="max-width: 600px">
    <div class="card">
        <div class="card-header">
            <h2 class="text-center mb-0">Confirm Delete</h2>
        </div>
        <div class="card-body">
            <h5 class="card-title">Are you sure you want to delete this food?</h5>
            <p class="card-text">
                <strong>Food Name:</strong> @Model.food.Name
            </p>
            <div class="alert alert-warning">
                <i class="bi bi-exclamation-triangle me-2"></i>
                Warning: This action cannot be undone.
            </div>
            <form method="post">
                <input type="hidden" asp-for="food.Id" />
                <div class="d-flex justify-content-between">
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash me-2"></i>Delete
                    </button>
                    <a class="btn btn-outline-secondary" asp-page="./ManageFoods">
                        <i class="bi bi-x-circle me-2"></i>Cancel
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

